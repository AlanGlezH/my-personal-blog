import styled from 'styled-components';
import StyledBarButton from './styledBarButton';

const BarButton = () => {
  return <StyledBarButton size='2rem'></StyledBarButton>;
};

export default BarButton;
